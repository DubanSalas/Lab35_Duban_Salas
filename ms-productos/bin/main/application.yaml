server:
  port: ${SERVER_PORT:8081}

spring:
  application:
    name: ms-productos
  r2dbc:
    url: r2dbc:postgresql://ep-morning-night-at61l5h9-pooler.c-9.us-east-1.aws.neon.tech/neondb?sslMode=require
    username: neondb_owner
    password: npg_uUDlyeM9qgr7

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: always
      probes:
        enabled: true
  health:
    readinessstate:
      enabled: true
    livenessstate:
      enabled: true
